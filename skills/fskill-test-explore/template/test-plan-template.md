# 测试方案

> 基于：`<PRD 文档名>`、技术分析文档、DDL 设计、任务拆分
> 生成日期：<YYYY-MM-DD>

---

## 功能边界

### 验证范围

- <模块1>：<功能点列表>
- <模块2>：<功能点列表>
- 端到端链路：<完整链路描述>

> **示例：**
> - 订单管理：订单创建/修改/查询/取消，订单状态机流转
> - 支付模块：支付回调接收、验签、状态更新、幂等校验
> - 端到端链路：用户下单→支付→回调→状态更新→通知商户

### 不验证范围

- <不验证项1>（原因）
- <不验证项2>（原因）

> **示例：**
> - 真实第三方支付联通性（使用 mock 模拟）
> - 管理后台 UI（P2 优先级，本次不含）

### 代码定位

| 层次 | 路径 | 说明 |
|---|---|---|
| Controller/API | `<路径>` | <说明> |
| Service/Domain | `<路径>` | <说明> |
| DAO/Mapper | `<路径>` | <说明> |
| MQ/Job/Listener | `<路径或无>` | <说明> |
| Config | `<配置来源>` | <说明> |
| 前端入口 | `<路径或无>` | <说明> |
| Mock | `<路径>` | <说明> |

> **示例：**
>
> | 层次 | 路径 | 说明 |
> |---|---|---|
> | Controller/API | `backend/app-service/src/main/java/.../controller/` | 订单相关 Controller |
> | Service/Domain | `backend/app-service/src/main/java/.../service/` | 订单业务 Service 层 |
> | DAO/Mapper | `backend/app-service/src/main/java/.../mapper/` | MyBatis-Plus Mapper |
> | 前端入口 | 无 | 本功能纯 API |

---

## 触发入口

| 入口类型 | 是否已实现 | 入口路径 | 备注 |
|---|---|---|---|
| HTTP API | 是/否 | `<路径>` | <说明> |
| MQ 消息 | 是/否 | `<路径>` | <说明> |
| 定时任务 | 是/否 | `<路径>` | <说明> |
| 内部服务 | 是/否 | `<路径>` | <说明> |
| 前端操作 | 是/否 | `<路径>` | <说明> |
| 数据库状态 | 是/否 | `<路径>` | <说明> |

> **示例：**
>
> | 入口类型 | 是否已实现 | 入口路径 | 备注 |
> |---|---|---|---|
> | HTTP API | 否 | `POST /api/order/create`, `/api/order/cancel` 等 5 个接口 | 商户通过网关调用 |
> | MQ 消息 | 否 | 支付回调 HTTP POST | 由第三方支付平台主动推送 |
> | 定时任务 | 否 | 超时订单自动取消（XXL-Job） | 每 5 分钟扫描 |
> | 前端操作 | 否 | — | 本功能无前端页面 |

### 触发入口结论

- **推荐入口**：<推荐入口及理由>
- **不推荐入口**：
  - <不推荐入口1>：<原因>

> **示例：**
> - **推荐入口**：`HTTP API`（主测试入口：商户调用的所有 CRUD 接口）+ `MQ 消息`（支付回调，验证状态流转）
> - **不推荐入口**：
>   - `定时任务`：超时扫描难以精确控制触发时机，建议直接构造过期数据验证

---

## 依赖服务

### 应用服务

| 应用 | 是否必需 | 启动方式 | 健康检查 |
|---|---|---|---|
| <应用名> | 是/否 | <启动命令> | <检查方式> |

> **示例：**
>
> | 应用 | 是否必需 | 启动方式 | 健康检查 |
> |---|---|---|---|
> | app-service | **是** | `bash harness/scripts/project/start-server.sh api` | `GET /actuator/health` |
> | app-admin | 否 | — | — |

### Docker 依赖

| 服务 | 是否必需 | 用途 |
|---|---|---|
| MySQL | 是/否 | <用途> |
| Redis | 是/否 | <用途> |
| Nacos | 是/否 | <用途> |
| RocketMQ | 是/否 | <用途> |
| mock-upstream | 是/否 | <用途> |

> **示例：**
>
> | 服务 | 是否必需 | 用途 |
> |---|---|---|
> | MySQL | 是 | 订单表数据存储 |
> | Redis | 是 | 分布式锁（防重复提交）、缓存 |
> | mock-upstream | 是 | 模拟第三方支付 API |

---

## 数据准备

| 名称 | 位置 | 初始化 | 清理 |
|---|---|---|---|
| <数据集名称> | `<文件路径>` | <初始化方式> | <清理方式> |

> **示例：**
>
> | 名称 | 位置 | 初始化 | 清理 |
> |---|---|---|---|
> | 订单创建测试数据 | `<featureRoot>/test/fixtures/db/tmp/TC-order-create-001.sql` | INSERT 订单、商品、用户等测试数据 | `DELETE FROM t_order WHERE tenant_code = 'test'` |

### 种子数据扩展

- 是否必需：**是/否**
- 基线变更：**是/否**
- 判断依据：<说明哪些 SQL 应进入 harness 基线，哪些只作为本迭代临时数据>
- 文件清单：
  - `<路径>` — <说明>

> **示例：**
> - 是否必需：**是**
> - 基线变更：**否**
> - 判断依据：订单正常流、金额为空、重复回调等数据只服务本次 `TC-xxx`，不具备跨功能复用价值，放入迭代临时 SQL；如果新增订单表结构被后续功能测试复用，则表结构应进入 `harness/tests/fixtures/db/schema.sql`。
> - 文件清单：
>   - `<featureRoot>/test/fixtures/db/tmp/TC-order-create-001.sql` — TC-001 订单创建正常流数据
>   - `<featureRoot>/test/fixtures/db/tmp/TC-order-create-002-invalid-amount.sql` — TC-002 金额为空边界数据
>
> **基线变更示例：**
> - 基线变更：**是**
> - 判断依据：新增订单状态字典会被订单创建、查询、取消、退款等后续功能测试复用，适合作为 harness 基线数据。
> - 文件清单：
>   - `harness/tests/fixtures/db/init_data.sql` — 追加订单状态字典数据

---

## Mock 策略

### Mock 1：<被模拟的外部服务名称>

- **方式**：<如何模拟>
- **断言要求**：
  - <断言1>
  - <断言2>

> **示例：**
> ### Mock 1：第三方支付 API
> - **方式**：mock-upstream 扩展路由，模拟 `pay.create`、`pay.query`、`pay.refund` 的请求响应
> - **断言要求**：
>   - mock 返回的交易号格式与真实接口一致
>   - mock 支持可配置的错误码返回（用于测试失败场景）
>   - mock 支持记录调用次数（用于验证幂等）

---

## 测试类型选择

| 类型 | 是否采用 | 理由 | 文件位置 |
|---|---|---|---|
| **<主类型>**（主） | 是 | <理由> | `<路径>` |
| <辅类型>（辅） | 是/否 | <理由> | `<路径>` |

**被拒绝的类型**：

- `<类型>`：<原因>

> **示例：**
>
> | 类型 | 是否采用 | 理由 | 文件位置 |
> |---|---|---|---|
> | **node_api**（主） | 是 | HTTP API 是主入口，Node.js 黑盒测试可直接验证请求/响应/DB 副作用 | `<featureRoot>/test/api/*.test.mjs` |
> | java_unit（辅） | 是 | 金额计算、参数校验等纯逻辑 | `backend/app-service/src/test/java/` |
> | manual_smoke | 否 | 所有接口均有 HTTP 入口 | — |
>
> **被拒绝的类型**：
> - `e2e`（含真实第三方支付）：无法在测试环境对接真实支付平台

---

## 覆盖等级

| 维度 | 值 |
|---|---|
| 目标覆盖等级 | `<level>` |
| 最低可接受等级 | `<level>` |
| 降级是否需用户批准 | **是** |

**理由**：<选择该等级的原因>

覆盖等级从低到高：`unit` → `service` → `integration` → `e2e`

> **示例：**
>
> | 维度 | 值 |
> |---|---|
> | 目标覆盖等级 | `integration`（集成） |
> | 最低可接受等级 | `integration`（集成） |
> | 降级是否需用户批准 | **是** |
>
> **理由**：涉及多表事务、分布式锁和外部 API 调用，必须在集成环境下验证端到端链路

---

## 不可替代断言

| ID | 断言内容 | 原因 |
|---|---|---|
| NNA-001 | <断言描述> | <原因> |
| NNA-002 | <断言描述> | <原因> |

> **示例：**
>
> | ID | 断言内容 | 原因 |
> |---|---|---|
> | NNA-001 | 创建订单后，ID 映射表中必须存在本系统 ID → 第三方 ID 的映射记录 | ID 映射是后续所有操作的基础 |
> | NNA-002 | 收到支付成功回调后，订单状态必须从 PENDING 变为 PAID，且支付记录表必须写入对应记录 | 支付成功是核心状态流转节点 |
> | NNA-003 | 同一交易号重复发送回调时，支付记录表中只存在一条记录（幂等性） | 第三方重试机制会导致重复投递 |
> | NNA-004 | 并发提交同一订单的修改请求时，第二次必须返回 BUSY 错误码 | 并发修改会导致状态不一致 |

---

## 降级路径

| 降级场景 | 允许条件 | 需用户批准 |
|---|---|---|
| <场景描述> | <条件> | 是 |

> **示例：**
>
> | 降级场景 | 允许条件 | 需用户批准 |
> |---|---|---|
> | 消息重试链路（8 次/25h）无法在自动化中等待真实时间 | 延迟消息不支持测试环境加速 | 是 |
> | 第三方验签逻辑在测试环境使用测试密钥或 bypass 模式 | 无法获取真实公钥证书 | 是 |

---

## 测试用例

### TC-001 · <场景名称>

| 维度 | 内容 |
|---|---|
| **场景** | <场景描述> |

**前置条件**：

- <条件1>
- <条件2>

**操作步骤**：

1. <步骤1>
2. <步骤2>

**预期结果**：

- <结果1>
- <结果2>

---

### TC-002 · <场景名称>

（同上格式）

> **示例：**
>
> ### TC-001 · 订单创建正常流程
>
> | 维度 | 内容 |
> |---|---|
> | **场景** | 订单创建正常流程 |
>
> **前置条件**：
>
> - 数据库中存在有效的商品和用户数据
> - mock 第三方支付接口返回成功
>
> **操作步骤**：
>
> 1. POST 订单创建接口（商品 ID + 用户 ID + 金额）
>
> **预期结果**：
>
> - 响应包含订单 ID
> - 订单表存在对应记录，状态为 PENDING
> - ID 映射表存在本系统 → 第三方映射
>
> ---
>
> ### TC-002 · 订单创建参数校验 — 金额为空
>
> | 维度 | 内容 |
> |---|---|
> | **场景** | 订单创建参数校验 — 金额为空 |
>
> **前置条件**：
>
> - 数据库中存在有效的商品和用户数据
>
> **操作步骤**：
>
> 1. POST 订单创建接口（金额字段为空）
>
> **预期结果**：
>
> - 响应错误码 INVALID_PARAM
> - 订单表无新增记录
>
> ---
>
> ### TC-003 · 支付回调幂等 — 重复发送相同回调
>
> | 维度 | 内容 |
> |---|---|
> | **场景** | 支付回调幂等 — 重复发送相同回调 |
>
> **前置条件**：
>
> - 已成功处理一次支付回调
>
> **操作步骤**：
>
> 1. 再次发送相同的支付成功回调
>
> **预期结果**：
>
> - 支付记录表中交易号不重复（仍然只有一条）
> - 响应成功（幂等处理）

---

## 执行进度

- [ ] 方案校验通过
- [ ] 环境启动完成
- [ ] TC-001 已实现
- [ ] TC-001 目标测试通过
- [ ] TC-001 NNA 覆盖
- [ ] 目标回归通过
- [ ] 全量回归通过
- [ ] 工程校验通过
- [ ] 清理完成

---

## 断言设计

### HTTP 断言

**状态码**：

| 状态码 | 含义 |
|---|---|
| <code> | <含义> |

**响应体**：

- <断言1>
- <断言2>

**错误码清单**：

| 错误码 | 说明 |
|---|---|
| <code> | <说明> |

> **示例：**
>
> **状态码**：
>
> | 状态码 | 含义 |
> |---|---|
> | 200 | 所有正常创建/查询/修改接口 |
> | 400 | 参数校验失败（INVALID_PARAM） |
> | 409 | 并发冲突（BUSY） |
> | 502 | 第三方 API 调用失败 |
>
> **响应体**：
>
> - 创建接口响应包含业务 ID
> - 错误响应包含 error_code 和 error_message
>
> **错误码清单**：
>
> | 错误码 | 说明 |
> |---|---|
> | INVALID_PARAM | 参数校验失败 |
> | BUSY | 正在处理中（分布式锁互斥） |
> | INVALID_STATUS | 当前状态不允许该操作 |

### 数据库断言

| 表名 | 断言 |
|---|---|
| <表名> | <断言内容> |

> **示例：**
>
> | 表名 | 断言 |
> |---|---|
> | t_order | 创建后存在对应记录；状态严格按状态机流转；金额单位正确 |
> | t_payment | 交易号唯一（幂等）；状态初始为 PROCESSING |
> | t_id_mapping | 本系统 ID + 类型联合唯一；每种实体创建后均写入映射 |

### Redis / MQ / Mock 断言

| 组件 | 断言 |
|---|---|
| <组件> | <断言内容> |

> **示例：**
>
> | 组件 | 断言 |
> |---|---|
> | 分布式锁（Redis） | 锁在操作期间存在；操作完成后释放；并发请求时第二个获取锁失败 |
> | mock（第三方 API） | 各接口调用次数可验证；支持返回可配置错误码 |

### 日志与监控断言

| 日志类型 | 说明 |
|---|---|
| <类型> | <说明> |

> **示例：**
>
> | 日志类型 | 说明 |
> |---|---|
> | 幂等冲突日志 | 同一交易号重复写入时记录日志但不报错 |
> | 第三方 API 调用失败日志 | 含第三方原始错误码 |
> | 事务回滚日志 | 任一步骤写入失败时回滚记录 |
