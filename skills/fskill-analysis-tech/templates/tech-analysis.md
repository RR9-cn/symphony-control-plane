<!-- 模板使用说明：
  - {{...}} 为占位符，根据输入材料替换
  - <!-- 指引: ... --> 为格式说明，生成时删除
  - > **示例：** 块为通用示例，仅供参考格式，实际内容根据输入材料生成
  - 按需章节根据 SKILL.md 章节裁剪规则决定是否启用
-->

## 1. 概览
### 1.1 核心链路全景
```plain
{{用 ASCII 图展示核心链路：外部系统 → 内部模块 → 外部系统，标注关键协议和交互方式}}
```

> **{{已有能力简述}}**：{{如签名验签、限流、日志等已有能力，一笔带过}}
>

> **示例：**
> ```plain
> ┌──────────┐                ┌──────────────────────┐                ┌──────────┐
> │          │  HTTPS/JSON    │    业务服务层          │   HTTPS/API    │          │
> │  客户端   │ ─────────────► │  订单/库存/用户 模块   │ ─────────────► │ 第三方平台 │
> │          │ ◄───────────── │                      │ ◄───────────── │          │
> └──────────┘   JSON 响应    └──────────┬───────────┘   JSON 响应    └──────────┘
>                                        │
>                                        ▼
>                              ┌──────────────────┐
>                              │   消息回调网关     │
>                              └──────────────────┘
> ```

### 1.2 模块与项目矩阵
<!-- 指引：根据任务拆分表格和步骤 2.5 的项目信息填充 -->
| 模块 | 业务 | 所属项目 | 开发 |
| --- | --- | --- | --- |
| {{M1}} | {{业务描述}} | {{项目名称}} | {{开发人员}} |
| {{M2}} | {{业务描述}} | {{项目名称}} | {{开发人员}} |


> **示例：**
> | 模块 | 业务 | 所属项目 | 开发 |
> | --- | --- | --- | --- |
> | M1 商品管理 | 商品 CRUD + 第三方 ID 映射 | order-service | Java 后端 |
> | M2 订单管理 | 订单全生命周期管理 | order-service | Java 后端 |

### 1.3 接口速览
<!-- 指引：按模块分组呈现，每个模块一个子表 -->
<!-- 指引：每个接口标注触发方和关联的核心流程章节 -->

#### {{模块 1 名称}}
| 接口 | 方法 | 说明 | 触发方 |
|------|------|------|--------|
| `{{接口标识}}` | POST | {{说明}} | {{服务商/第三方推送/内部}} |

#### {{模块 2 名称}}
| 接口 | 方法 | 说明 | 触发方 | 关联流程 |
|------|------|------|--------|----------|
| `{{接口标识}}` | POST | {{说明}} | {{触发方}} | → {{流程章节}} |

> **示例：**
>
> #### M1 商品管理
> | 接口 | 方法 | 说明 | 触发方 |
> |------|------|------|--------|
> | `product.create` | POST | 创建商品 | 客户端 |
> | `product.query` | POST | 查询商品 | 客户端 |
>
> #### M2 订单管理
> | 接口 | 方法 | 说明 | 触发方 | 关联流程 |
> |------|------|------|--------|----------|
> | `order.create` | POST | 创建订单 | 客户端 | → 5.2 订单创建 |
> | `order.cancel` | POST | 取消订单 | 客户端 | → 5.3 订单取消 |
>
> #### M3 消息回调
> | 接口 | 方法 | 说明 | 触发方 | 关联流程 |
> |------|------|------|--------|----------|
> | 第三方 → 回调网关 | HTTP POST | 接收状态变更通知 | 第三方推送 | → 5.4 回调处理 |

### 1.4 对齐结论摘要
<!-- 指引：承接步骤 3/4/4.5 的双轨对齐结果，只写影响技术方案的结论，不复述普通产品细节 -->

#### 产品向业务约束
| 约束/决策 | 结论 | 技术影响 | 来源 |
| --- | --- | --- | --- |
| {{范围/业务语义/状态/风险接受}} | {{结论}} | {{影响接口/DDL/状态机/权限/流程}} | {{继承/Agent 默认决策/用户确认/待定/风险}} |

#### 技术向核心决策
| 设计点 | 决策 | 技术影响 | 来源 |
| --- | --- | --- | --- |
| {{权限/短信/审计/事务后副作用等}} | {{组件/模式/边界/兜底}} | {{扩展性/风险/开发落点}} | {{Agent 默认决策/用户确认/待定}} |

#### 技术方案选型与一致性闭环
| 核心流程 | 推荐实现路径 | 不采用的路径及原因 | 一致性/失败兜底 |
| --- | --- | --- | --- |
| {{下单/支付/入账/注销等}} | {{同步接口 + MQ + 定时补偿 + afterCommit 等}} | {{为什么不用纯同步/纯定时/直接调用等}} | {{事务/锁/幂等/补偿/对账/告警/人工处理}} |

---

## 2. 系统架构
### 2.1 分层架构
```plain
{{用 ASCII 图展示分层架构：接入层 → 业务层 → 关键中间件 → 数据层}}
```

> **示例：**
> ```plain
> ┌─────────────────────────────────────────────────┐
> │              接入层（API 网关）                    │
> │  签名验签 → 限流 → 路由 → 租户鉴权                │
> └──────────────────────────┬──────────────────────┘
>                            │
> ┌──────────────────────────▼──────────────────────┐
> │              业务层（核心模块）                    │
> │  ┌────────┐ ┌────────┐ ┌────────┐               │
> │  │商品服务 │ │订单服务 │ │用户服务 │               │
> │  └────┬───┘ └────┬───┘ └────┬───┘               │
> │       └──────────┼──────────┘                    │
> │            ┌─────▼─────┐                         │
> │            │ 回调网关   │                         │
> │            └───────────┘                         │
> └──────────────────────────┬──────────────────────┘
>                            │
> ┌──────────────────────────▼──────────────────────┐
> │              数据层                               │
> │  MySQL 8（业务表）│ Redis（分布式锁/缓存）         │
> └─────────────────────────────────────────────────┘
> ```

### 2.2 关键组件设计
<!-- 指引：描述架构中最重要的组件/网关/中间件的设计，如出口网关、消息中间件、缓存策略等 -->
<!-- 指引：如果架构中有统一封装层（如出口网关、SDK Client），用 ASCII 图展示其内部结构 -->

**{{组件名称}}**：{{一句话说明职责}}

```plain
{{用 ASCII 图展示组件内部结构：入口 → 核心能力 → 出口}}
```

---

## 3. 核心技术设计与扩展约束
<!-- 指引：本章必写。用于承接横切能力、高敏能力、强扩展点、统一组件边界，防止后续编码写散。 -->
<!-- 指引：如果本次范围没有独立核心技术设计点，也要写明“本次无独立设计点”及原因。 -->

### 3.1 设计点总览
| 设计点 | 类型 | 为什么单独设计 | 推荐实现形态 | 关联模块/流程 |
| --- | --- | --- | --- | --- |
| {{权限/幂等/状态流转/缓存等}} | {{横切能力/高敏能力/强扩展点/统一基础设施}} | {{写散后的风险、未来扩展概率、敏感程度}} | {{自定义注解/AOP/策略模式/模板方法/Manager/CacheManager 等}} | {{模块或流程章节}} |

### 3.2 {{设计点名称}}
**为什么单独设计**：{{说明横切范围、敏感程度、未来扩展概率、写散后的后果}}

| 组件/模式 | 职责 | 放置位置 | 调用方 | 禁止事项 |
| --- | --- | --- | --- | --- |
| {{组件或模式}} | {{职责}} | {{模块/包}} | {{谁调用}} | {{不能怎么写，例如禁止 Controller 手写权限判断、禁止业务 Service 直接拼 Redis key}} |

**扩展方式**：
- {{新增策略/枚举/注解/模板时怎么扩展}}

**失败与兜底**：
- {{组件失败时的降级、告警、回滚或不回滚策略}}

### 3.N {{更多核心技术设计点}}
<!-- 指引：优先覆盖鉴权/权限/审计/日志/风控/数据权限/脱敏/限流/密码/手机号/账号注销/状态机/缓存/幂等/分布式锁/事务后置动作/外部渠道适配/通知模板等容易写散或未来高概率扩展的内容。 -->

---

## 4. 数据模型
### 4.1 核心 ER
```plain
{{用 ASCII 图展示核心表之间的关系：已有表 → 新建表 → 关联表，标注关联字段}}
```

**关联方式**：{{说明表间关联方式，如"所有表间关联用 `*_biz_no`，不用自增 id"}}

### 4.2 表结构速览
<!-- 指引：按业务域分组，如"核心业务表"、"核心交易表"等 -->

#### {{业务域 1}}
| 表 | 用途 | 业务主键 | 核心字段 | 关联 |
| --- | --- | --- | --- | --- |
| `{{表名}}` | {{一句话用途}} | `{{业务主键字段}}` | {{核心字段列表，状态字段加 ★}} | → {{关联表}} |


#### {{业务域 2}}
| 表 | 用途 | 业务主键 | 核心字段 | 关联 |
| --- | --- | --- | --- | --- |
| `{{表名}}` | {{一句话用途}} | `{{业务主键字段}}` | {{核心字段列表}} | → {{关联表}} |


#### 已有表（不改造，仅关联）
<!-- 指引：如果有已有表参与关联，列出；无则省略本小节 -->
| 表 | 用途 | 关联方式 |
| --- | --- | --- |
| `{{已有表名}}` | {{用途}} | {{如何与新表关联}} |

### 4.3 查询与索引评估
<!-- 指引：逐表检查已确认业务查询、排序、分页和关联访问。遵循最小索引原则：仅列出唯一索引、规范强制索引和明确业务必需的索引；无新增索引时明确写“无”，不得为预防性优化添加索引。 -->

| 表 | 业务查询场景 | 查询/排序字段 | 索引结论 | 必要性 |
| --- | --- | --- | --- | --- |
| `{{表名}}` | {{列表查询/详情关联/定时扫描等}} | `{{字段}}` | {{复用现有索引 / 新增 `idx_xxx` / 无新增}} | {{说明为何业务必须，或为何无需新增}} |

**新增索引清单**：{{无；或列出索引定义及对应查询场景。}}


### 4.4 核心状态机
<!-- 指引：采用三层结构：① 状态图（简洁）② 状态流转明细表（含触发过程 + 接口关联）③ 需开发接口清单 -->
<!-- 指引：如果有状态流转的实体（如订单状态、审批状态），画出状态机；无则省略 -->

#### {{状态机名称}}

```plain
{{用 ASCII 图展示状态机：只画状态 + 箭头，保持简洁，不塞细节}}
```

> **示例：**
> ```plain
> [创建] ──► PENDING ──► ACTIVE ◄────────┐
>               │           │             │
>           失败 │      周期扣款│       恢复 │
>               ▼           ▼             │
>            FAILED    RENEWED        CANCEL_PENDING ──► CANCELED
> ```

#### 状态流转明细（含触发过程 + 接口关联）

**从 {{状态A}} 出发**

| 目标状态 | 触发场景 | 触发入口 | 关联流程 | 前置条件 |
|----------|----------|----------|----------|----------|
| → {{状态B}} | {{描述}} | {{入口}} — **需开发：{{接口名}}** | → {{章节}} | {{条件}} |
| → {{状态C}} | {{描述}} | {{入口}} — **复用：{{已有接口}}** | → {{章节}} | {{条件}} |

> **示例：**
>
> **从 PENDING 出发**
>
> | 目标状态 | 触发场景 | 触发入口 | 关联流程 | 前置条件 |
> |----------|----------|----------|----------|----------|
> | → ACTIVE | 支付成功，第三方推送支付成功通知 | 第三方 → 回调网关（payment_success）— **需开发：支付回调处理接口** | → 5.2 支付处理 | 无 |
> | → FAILED | 支付超时或失败，第三方推送失败通知 | 第三方 → 回调网关（payment_failed）— **需开发：支付回调处理接口** | → 5.2 支付处理 | 无 |
>
> **从 ACTIVE 出发**
>
> | 目标状态 | 触发场景 | 触发入口 | 关联流程 | 前置条件 |
> |----------|----------|----------|----------|----------|
> | → CANCEL_PENDING | 用户发起取消请求 | 客户端 → `order.cancel` — **需开发：订单取消接口** | → 5.3 订单取消 | status=ACTIVE |
> | → RENEWED | 周期续费成功 | 第三方 → 回调网关（renew_success）— **复用：支付回调处理接口** | → 5.2 支付处理 | 当前周期结束 |
>
> **从 CANCEL_PENDING 出发**
>
> | 目标状态 | 触发场景 | 触发入口 | 关联流程 | 前置条件 |
> |----------|----------|----------|----------|----------|
> | → CANCELED | 取消生效（退款完成） | 第三方 → 回调网关（refund_done）— **复用：退款回调处理接口** | → 5.5 退款处理 | 退款成功 |
> | → ACTIVE | 用户撤销取消 | 客户端 → `order.revert_cancel` — **需开发：撤销取消接口** | → 5.3 订单取消 | 当前周期未结束 |

#### {{状态机名称}} → 需开发接口清单
<!-- 指引：从状态机倒推出需要开发的接口，便于技术经理分配任务 -->
| 需开发接口 | 职责 | 驱动的状态流转 | 触发方 |
|-----------|------|---------------|--------|
| **{{接口名}}** | {{职责}} | {{流转}} | {{触发方}} |

> **示例：**
>
> #### 订单状态机 → 需开发接口清单
> | 需开发接口 | 职责 | 驱动的状态流转 | 触发方 |
> |-----------|------|---------------|--------|
> | **支付回调处理接口** | 接收第三方支付结果，驱动订单状态流转 | PENDING → ACTIVE / FAILED | 第三方推送 |
> | **订单取消接口** | 用户发起取消，驱动 ACTIVE → CANCEL_PENDING | ACTIVE → CANCEL_PENDING | 客户端 |
> | **退款回调处理接口** | 接收第三方退款结果，驱动最终取消 | CANCEL_PENDING → CANCELED | 第三方推送 |
> | **撤销取消接口** | 用户撤销取消，恢复订单 | CANCEL_PENDING → ACTIVE | 客户端 |

### 4.4 关键设计约定
| 约定 | 说明 |
| --- | --- |
| {{约定 1}} | {{说明}} |
| {{约定 2}} | {{说明}} |


---

## 5. 核心流程
<!-- 指引：每个核心流程 = PlantUML 时序图 + 步骤卡片（Step Card） -->
<!-- 指引：PlantUML 统一用 ```puml 代码块，参与方用中文别名 -->
<!-- 指引：步骤卡片用编号步骤 + 内联标签替代传统操作矩阵表格 -->
<!-- 指引：校验步骤必须展开通过/不通过两条路径 -->
<!-- 指引：数据写入步骤必须展开到字段级别 -->
<!-- 指引：外部调用必须标注成功/失败两条路径 -->

### 5.1 {{流程名称}}
```puml
@startuml
{{PlantUML 时序图源码}}
@enduml
```

#### 技术实现路径
<!-- 指引：说明本流程选择同步接口、MQ、定时任务、afterCommit、Outbox/本地消息表、状态机/流程编排的组合方式，以及为什么不用其他路径 -->

| 环节 | 实现路径 | 选择理由 | 一致性/兜底 |
| --- | --- | --- | --- |
| {{入口/数据变更/异步事件/外部回调/补偿任务}} | {{同步/MQ/定时/afterCommit/Outbox/状态机}} | {{理由}} | {{事务/锁/幂等/补偿/对账/告警/人工处理}} |

#### 操作步骤

**Step 1 · {{步骤名称}}**
- `校验` {{校验条件}}

<!-- 指引：校验步骤展开为通过/不通过两条路径 -->
| 校验项 | 通过 | 不通过 |
|--------|------|--------|
| {{校验项}} | 继续 | 返回错误：`{{错误码}}`，{{原因}} |

**Step 2 · {{步骤名称}}**
- `调外部` {{外部接口名}}

<!-- 指引：外部调用展开为成功/失败两条路径 -->
| 结果 | 处理 |
|------|------|
| 成功 | {{成功处理}} |
| 失败 | 返回错误：`{{错误码}}`，{{失败处理}} |

**Step 3 · {{步骤名称}}** `事务: {{事务边界说明}}`
- `INSERT` **{{表名}}**

<!-- 指引：数据写入展开到字段级别 -->
| 字段 | 值 | 说明 |
|------|---|------|
| {{字段名}} | {{值}} | {{说明}} |

- `UPDATE` **{{表名}}** SET {{字段=值}}

> 事务异常：{{回滚策略说明}}

> **示例（订单创建流程）：**
>
> **Step 1 · 参数校验**
>
> | 校验项 | 通过 | 不通过 |
> |--------|------|--------|
> | product_id 存在且 status=1 | 继续 | 返回错误：`INVALID_PRODUCT`，商品不存在或已下架 |
> | user_id 存在 | 继续 | 返回错误：`USER_NOT_FOUND` |
> | quantity > 0 且 ≤ 库存 | 继续 | 返回错误：`STOCK_NOT_ENOUGH` |
>
> **Step 2 · 调用第三方支付接口**
> - `调外部` payment.create
>
> | 结果 | 处理 |
> |------|------|
> | 成功 | 获取 payment_id 和支付跳转链接，继续 |
> | 失败 | 返回错误：`PAYMENT_API_ERROR` + 原始错误码，**不写入本地数据**，流程终止 |
>
> **Step 3 · 写入订单主表** `事务: 订单+订单项+映射，三表同事务`
> - `INSERT` **t_order**
>
> | 字段 | 值 | 说明 |
> |------|---|------|
> | order_id | 生成业务 ID | varchar(32) 业务主键 |
> | tenant_code | 当前租户 | 来自网关鉴权 |
> | user_id | 入参 user_id | 关联用户表 |
> | order_status | PENDING | 等待支付确认 |
> | total_amount | 计算得出 | 单位：分 |
> | payment_id | Step 2 返回的 ID | 第三方支付 ID |
>
> - `INSERT` **t_order_item** `同事务`
>
> | 字段 | 值 | 说明 |
> |------|---|------|
> | item_id | 生成业务 ID | varchar(32) 业务主键 |
> | order_id | Step 3 的 order_id | 关联订单主表 |
> | product_id | 入参 product_id | 关联商品表 |
> | quantity | 入参 quantity | 购买数量 |
>
> > 事务异常：Step 3 任一写入失败 → 整体回滚，返回 `INTERNAL_ERROR`。

### 5.2 {{流程名称（含分支）}}
<!-- 指引：如果流程有多个分支，按分支分别列出步骤卡片 -->
```puml
@startuml
{{PlantUML 时序图源码}}
@enduml
```

#### 公共前置步骤

**Step 1 · {{公共步骤}}**
{{同上格式}}

#### 分支 A · {{分支名称}}

**Step 2 · {{步骤名称}}**
{{同上格式}}

#### 分支 B · {{分支名称}}

**Step 2 · {{步骤名称}}**
{{同上格式}}

> **示例（回调处理流程，按事件类型分支）：**
>
> #### 公共前置步骤
>
> **Step 1 · 接收通知并验签**
>
> | 校验项 | 通过 | 不通过 |
> |--------|------|--------|
> | 第三方公钥验签 | 继续 | 返回 HTTP 400，**不写入任何数据**，流程终止 |
>
> - `INSERT` **t_callback_log**
>
> | 字段 | 值 | 说明 |
> |------|---|------|
> | log_id | 生成业务 ID | varchar(32) 业务主键 |
> | event_type | 通知中的 event_type | 事件类型 |
> | process_status | 0 | 已接收，待处理 |
> | raw_body | 原始 JSON | 保留完整报文 |
>
> | 写入结果 | 处理 |
> |----------|------|
> | 成功 | 继续 |
> | 唯一索引冲突（重复通知） | 记录日志，返回 HTTP 200，**跳过后续步骤**（幂等保护） |
>
> **Step 2 · 锁定业务记录**
> - `锁` SELECT FOR UPDATE t_order WHERE order_id=?
>
> | 校验项 | 通过 | 不通过 |
> |--------|------|--------|
> | 订单记录存在 | 继续 | 记录异常日志，process_status 保持 0，等待重试 |
>
> #### 分支 A · event_type = payment_success（支付成功）
>
> **Step 3 · 更新订单状态** `事务: 日志+订单同事务`
> - `UPDATE` **t_order** SET order_status = ACTIVE, paid_time = now()
>
> **Step 4 · 更新回调日志**
> - `UPDATE` **t_callback_log** SET process_status = 1
>
> #### 分支 B · event_type = payment_failed（支付失败）
>
> **Step 3 · 更新订单状态** `事务: 日志+订单同事务`
> - `UPDATE` **t_order** SET order_status = FAILED, failed_time = now()
>
> **Step 4 · 更新回调日志**
> - `UPDATE` **t_callback_log** SET process_status = 1

---

### 5.N {{更多核心流程...}}
<!-- 指引：根据材料识别的核心流程数量，通常 3-6 个 -->
<!-- 指引：每个流程重复上述格式：PlantUML + 步骤卡片 -->


---

<!-- ↓↓↓ 以下为按需章节，根据 SKILL.md 章节裁剪规则决定是否启用 ↓↓↓ -->

## N-1. 已有系统集成
<!-- 启用条件：DDL 中存在已有表关联、或 PRD 提到与已有系统对接 -->
<!-- 关注点：新表与旧表如何打通、同步失败的兜底、幂等的边界在哪里 -->

### N-1.1 {{集成点 1}}
**{{判断依据/触发条件}}**：{{说明什么情况下触发集成}}

**集成流程**：

```plain
{{用 ASCII 图展示集成流程：触发条件 → 查询/同步 → 写入 → 幂等}}
```

**关联方式**：{{说明新旧数据的关联方式}}

**触发场景：**

| 场景 | 触发条件 | 数据来源 |
| --- | --- | --- |
| {{场景 1}} | {{条件}} | {{来源}} |


### N-1.2 {{集成点 2}}
{{同上格式}}


---

## N. 商业影响分析
<!-- 启用条件：涉及资金流转、分账结算、佣金变更、或对已有业务流程有冲击 -->
<!-- 关注点：新能力是否打破了旧流程、哪些原有收益渠道受影响、替代路径是什么 -->

### N.1 原有方案：{{原有方案名称}}
{{原有方案的业务流转说明}}

```plain
{{用 ASCII 图展示原有业务流转路径}}
```

**特点**：{{原有方案的特点}}

### N.2 新方案的差异
{{新方案的业务流转说明}}

```plain
{{用 ASCII 图展示新方案业务流转路径}}
```

**结论**：{{新方案对业务的影响，以及替代方案}}

---

<!-- ↑↑↑ 以上为按需章节 ↑↑↑ -->

## N. 关键风险与应对
<!-- 指引：始终作为最后一个章节 -->
| # | 风险 | 概率 | 影响 | 应对 |
| --- | --- | --- | --- | --- |
| R1 | {{风险描述}} | {{高/中/低}} | {{高/中/低}} | {{应对方案}} |


### N.1 已知不可规避风险
| # | 风险场景 | 后果 | 原因 |
| --- | --- | --- | --- |
| R{{N}} | {{风险场景描述}} | **{{后果}}** | {{根因}} |


> {{风险根因总结说明}}
>

---

> 基于：{{列出输入材料名称和版本}}
>
