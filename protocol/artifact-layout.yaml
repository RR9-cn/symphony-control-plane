version: 2
scope: issue_workspace
path_style: repository_relative_posix
rules:
  - paths must be relative to the persistent Issue workspace
  - absolute paths, backslashes, empty segments, and parent traversal are forbidden
  - artifacts are optional evidence and never represent lifecycle handoffs
recommended_roots:
  - docs/
  - test/
  - reports/
  - artifacts/
